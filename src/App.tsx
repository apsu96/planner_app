import styled from "styled-components";

const AppContainer = styled.div`
  min-height: 100vh;
  background-color: #E5E5E5;
`

function App() {
  return (
    <AppContainer>
    </AppContainer>
  );
}

export default App;
